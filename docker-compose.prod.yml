version: '3.8'
services:
  app:
    container_name: workspace-server
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    depends_on:
      - mysql
    ports:
      - '8000:8000'
    env_file:
      - .env
    volumes:
      - /usr/src/app/node_modules
  mysql:
    image: mysql:8.0
    container_name: workspace-db
    restart: always
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: zhdlsshfoqkd12!ZHDLS
      MYSQL_DATABASE: workspace
      MYSQL_PASSWORD: zhdlsshfoqkd12!ZHDLS
